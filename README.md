# ttf_to_png-glyph

A tool to convert TrueType Font (TTF) files into PNG images of individual glyphs.

## Features

- Extract individual glyphs from TTF font files
- Export glyphs as PNG images
- Customizable output size and format
